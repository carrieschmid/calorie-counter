import React from 'react';

import classes from './Workout.module.css';

const workout = ( props ) => (

    <div>
        <p>Activity: Walking(1)</p>
        <p>Count: <strong>100</strong></p>
    </div>
);

export default workout;